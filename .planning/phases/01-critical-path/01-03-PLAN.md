---
phase: 01-critical-path
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - workflows/speed-to-lead-main.json
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03, INGEST-04]

must_haves:
  truths:
    - "A POST to the webhook URL returns HTTP 200 immediately (before any processing)"
    - "The raw lead payload is visible in the n8n execution log for every received lead"
    - "A lead with an invalid google_key does not proceed past the IF node"
    - "Sending the same lead_id twice results in only one execution proceeding past Remove Duplicates"
    - "Extracted fields (name, phone, email, request) are present in node output after Code: Extract Lead Fields"
  artifacts:
    - path: "workflows/speed-to-lead-main.json"
      provides: "Main workflow with complete ingestion layer: webhook + log + key validation + dedup + field extraction"
      contains: "google_key === $env.GOOGLE_KEY"
  key_links:
    - from: "workflows/speed-to-lead-main.json (Webhook node)"
      to: "Code: Log Raw Payload"
      via: "n8n node connection"
      pattern: "responseMode.*onReceived"
    - from: "Code: Log Raw Payload"
      to: "$getWorkflowStaticData"
      via: "JS staticData write"
      pattern: "getWorkflowStaticData"
    - from: "IF: google_key valid?"
      to: "$env.GOOGLE_KEY"
      via: "n8n environment variable"
      pattern: "GOOGLE_KEY"
    - from: "Remove Duplicates: lead_id"
      to: "lead_id field"
      via: "dedup key configuration"
      pattern: "lead_id"
---

<objective>
Implement the complete ingestion layer in the main workflow: webhook → raw log (with static data store) → google_key validation → lead_id deduplication → field extraction from user_column_data.

Purpose: This is the reliability and security foundation. Every subsequent step depends on this layer having run correctly. After this plan, a test lead can be sent and traced end-to-end through ingestion in n8n's execution log.
Output: workflows/speed-to-lead-main.json with ingestion layer fully implemented (4 nodes configured).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-critical-path/01-RESEARCH.md
@.planning/phases/01-critical-path/01-01-SUMMARY.md
@.planning/phases/01-critical-path/01-02-SUMMARY.md

<interfaces>
<!-- Existing skeleton nodes to modify (from Plan 02 output) -->
Node names as created in Plan 02:
  "Webhook: Google Ads Lead"       → set responseMode, path
  "Code: Log Raw Payload"          → implement raw log + staticData write
  "IF: google_key valid?"          → implement google_key comparison condition
  "Remove Duplicates: lead_id"     → configure operation, value, scope, history
  "Code: Extract Lead Fields"      → implement user_column_data parsing

<!-- Google Ads webhook payload structure -->
Top-level: lead_id, google_key, is_test, lead_submit_time, user_column_data[]
user_column_data item: { column_id, column_name, string_value }
Standard column_name values: "PHONE_NUMBER", "EMAIL", "FULL_NAME"
Custom question column_name: "demande" (Baptiste's configured question label)

<!-- n8n IF node condition format (typeVersion 2) -->
{
  "conditions": {
    "options": { "leftValue": "", "caseSensitive": true, "typeValidation": "strict" },
    "combinator": "and",
    "conditions": [
      {
        "leftValue": "={{ $json.google_key }}",
        "rightValue": "={{ $env.GOOGLE_KEY }}",
        "operator": { "type": "string", "operation": "equals" }
      }
    ]
  }
}

<!-- n8n Remove Duplicates node (typeVersion 1) parameters -->
{
  "operation": "removeDuplicateInputItems",
  "compare": "selectedFields",
  "fields": "lead_id"
}
Note: "Remove Items Processed in Previous Executions" operation name in UI = "removeDuplicateInputItems"
IMPORTANT: Research doc says operation is "Remove Items Processed in Previous Executions" mode.
The correct parameter value for cross-execution dedup is: "operation": "removeItemsSeenInPreviousExecutions"
Fields to compare: "lead_id"
Scope: "node"
History size: 10000
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Webhook node and Code: Log Raw Payload</name>
  <files>
    workflows/speed-to-lead-main.json
  </files>
  <read_first>
    - workflows/speed-to-lead-main.json (read current skeleton — understand existing node structure before modifying)
    - .planning/phases/01-critical-path/01-RESEARCH.md (sections: "Pattern 1: Webhook — Return 200 Immediately", "Pattern 3: Raw Payload Logging", "Pattern 8: Error Fallback — Error Fallback Static Data Pattern")
  </read_first>
  <action>
Edit `workflows/speed-to-lead-main.json`. Modify TWO nodes by updating their `parameters` field. Do not change node ids, names, positions, or connections.

**Node: "Webhook: Google Ads Lead"** — set to return 200 immediately:
```json
"parameters": {
  "path": "dupont-plomberie",
  "responseMode": "onReceived",
  "options": {}
}
```
Note: `responseMode: "onReceived"` is the n8n 2.x API name for "Respond immediately". This makes n8n return HTTP 200 with `{"message":"Workflow got started"}` before any other nodes execute.

**Node: "Code: Log Raw Payload"** — implement raw logging + staticData store:
```json
"parameters": {
  "jsCode": "// INGEST-03: Log raw payload for audit/debug\n// NOTIF-04 prep: Store last lead in staticData for error recovery\nconst lead = $input.first().json;\n\n// Log to n8n execution log (visible in Executions view for 7 days)\nconsole.log('RAW_LEAD:', JSON.stringify(lead));\n\n// Store in workflow static data so Error Handler workflow can recover it\n// This survives even if subsequent nodes fail\nconst staticData = $getWorkflowStaticData('global');\nstaticData.lastLead = JSON.stringify(lead);\nstaticData.lastLeadTime = new Date().toISOString();\n\n// Pass through unchanged — do not modify the payload\nreturn $input.all();"
}
```

Write the complete updated JSON file preserving all other nodes and the connections object unchanged.
  </action>
  <verify>
    <automated>
      python3 -c "
import json
w = json.load(open('workflows/speed-to-lead-main.json'))
nodes = {n['name']: n for n in w['nodes']}
# Webhook check
wh = nodes['Webhook: Google Ads Lead']
assert wh['parameters']['responseMode'] == 'onReceived', 'responseMode must be onReceived'
assert wh['parameters']['path'] == 'dupont-plomberie', 'path must be dupont-plomberie'
# Log node check
log = nodes['Code: Log Raw Payload']
assert 'getWorkflowStaticData' in log['parameters']['jsCode'], 'must store to staticData'
assert 'lastLead' in log['parameters']['jsCode'], 'must store lastLead key'
assert 'return \$input.all()' in log['parameters']['jsCode'].replace('\\n',' '), 'must pass through'
print('Webhook and log nodes OK')
"
    </automated>
  </verify>
  <done>
    Webhook node has responseMode "onReceived" and path "dupont-plomberie". Code: Log Raw Payload has jsCode that calls getWorkflowStaticData('global'), sets staticData.lastLead, calls console.log with JSON.stringify(lead), and returns $input.all().
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement google_key validation, deduplication, and field extraction</name>
  <files>
    workflows/speed-to-lead-main.json
  </files>
  <read_first>
    - workflows/speed-to-lead-main.json (current state after Task 1)
    - .planning/phases/01-critical-path/01-RESEARCH.md (sections: "Pattern 2: google_key Validation", "Pattern 4: lead_id Deduplication", "Code Examples > Extract user_column_data Fields")
  </read_first>
  <action>
Edit `workflows/speed-to-lead-main.json`. Modify THREE more nodes. Preserve all other nodes, connections, and existing node parameters from Task 1.

**Node: "IF: google_key valid?"** — compare body field to env var:
```json
"parameters": {
  "conditions": {
    "options": {
      "leftValue": "",
      "caseSensitive": true,
      "typeValidation": "strict"
    },
    "combinator": "and",
    "conditions": [
      {
        "id": "condition-google-key",
        "leftValue": "={{ $json.google_key }}",
        "rightValue": "={{ $env.GOOGLE_KEY }}",
        "operator": {
          "type": "string",
          "operation": "equals",
          "name": "filter.operator.equals"
        }
      }
    ]
  },
  "looseTypeValidation": false
}
```
IMPORTANT: `$json.google_key` reads from the REQUEST BODY (not headers). This is correct per Google Ads webhook specification. TRUE branch (index 0) → proceeds to dedup. FALSE branch (index 1) → no connection (execution stops silently, 200 already returned).

**Node: "Remove Duplicates: lead_id"** — cross-execution deduplication:
```json
"parameters": {
  "operation": "removeItemsSeenInPreviousExecutions",
  "deduplicationKey": "={{ $json.lead_id }}",
  "options": {
    "historySize": 10000
  }
}
```
TRUE output (index 0, new lead) → proceeds to extract fields. FALSE output (index 1, seen lead) → no connection (execution stops silently).

**Node: "Code: Extract Lead Fields"** — parse user_column_data array:
```json
"parameters": {
  "jsCode": "// Parse Google Ads Lead Form nested array into flat fields\n// Source: https://developers.google.com/google-ads/webhook/docs/implementation\nconst lead = $input.first().json;\nconst fields = {};\n\nfor (const col of lead.user_column_data || []) {\n  fields[col.column_name] = col.string_value;\n}\n\n// is_test check: log-only mode, no sends for test leads\n// Note: deduplication still runs for is_test leads — intentional\nconst isTest = lead.is_test === true;\n\nreturn [{\n  json: {\n    lead_id: lead.lead_id,\n    is_test: isTest,\n    phone: fields['PHONE_NUMBER'] || null,\n    email: fields['EMAIL'] || null,\n    name: fields['FULL_NAME'] || 'Prospect',\n    request: fields['demande'] || fields['message'] || fields['MESSAGE'] || '',\n    submit_time: lead.lead_submit_time,\n    raw: lead\n  }\n}];"
}
```

Write the complete updated JSON file with all five nodes now configured (Task 1 nodes preserved, three new nodes added).
  </action>
  <verify>
    <automated>
      python3 -c "
import json
w = json.load(open('workflows/speed-to-lead-main.json'))
nodes = {n['name']: n for n in w['nodes']}
# Key validation
kv = nodes['IF: google_key valid?']
cond_str = json.dumps(kv['parameters'])
assert '\$json.google_key' in cond_str, 'must read from body not headers'
assert '\$env.GOOGLE_KEY' in cond_str, 'must compare to env var'
# Dedup
dd = nodes['Remove Duplicates: lead_id']
assert 'removeItemsSeenInPreviousExecutions' in json.dumps(dd['parameters']), 'must use cross-execution dedup'
assert 'lead_id' in json.dumps(dd['parameters']), 'must dedup on lead_id'
# Extract
ex = nodes['Code: Extract Lead Fields']
code = ex['parameters']['jsCode']
assert 'user_column_data' in code, 'must parse user_column_data'
assert 'PHONE_NUMBER' in code, 'must extract phone'
assert 'is_test' in code, 'must handle is_test flag'
assert 'return [{' in code.replace('\\n', ''), 'must return flat object'
# Webhook and log still OK from task 1
wh = nodes['Webhook: Google Ads Lead']
assert wh['parameters']['responseMode'] == 'onReceived'
print('All ingestion nodes configured correctly')
"
    </automated>
  </verify>
  <done>
    IF node compares $json.google_key (body field) to $env.GOOGLE_KEY (env var) using string equals. Remove Duplicates uses "removeItemsSeenInPreviousExecutions" operation with lead_id as dedup key and historySize 10000. Code: Extract Lead Fields parses user_column_data into flat fields (lead_id, is_test, phone, email, name, request, submit_time, raw). All nodes from Task 1 remain unchanged.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Ingestion layer: webhook (returns 200 immediately) → raw log (writes to execution log and staticData) → google_key validation (stops invalid keys) → deduplication (removes seen lead_ids) → field extraction (parses user_column_data).
  </what-built>
  <how-to-verify>
    1. Start n8n locally: `n8n start`
    2. In n8n UI: import `workflows/speed-to-lead-main.json` (Settings > Import workflow)
    3. Set environment variable GOOGLE_KEY in n8n (Settings > Environment Variables or via .env): use the value "REPLACE_WITH_YOUR_GOOGLE_KEY" to match test fixtures
    4. Open the workflow — keep it in OPEN (not activated) mode to use webhook-test URL
    5. Send happy-path lead: `GOOGLE_KEY=REPLACE_WITH_YOUR_GOOGLE_KEY ./tests/test-webhook.sh happy`
    6. In n8n Executions view: open the execution and verify:
       - "Code: Log Raw Payload" output shows full JSON including lead_id, google_key, user_column_data
       - "IF: google_key valid?" TRUE branch was taken
       - "Remove Duplicates: lead_id" output shows 1 item (new lead passed through)
       - "Code: Extract Lead Fields" output shows flat object with phone: "+33612345678", name: "Marie Dupont", request: "Fuite d'eau sous l'évier..."
    7. Send invalid key: `./tests/test-webhook.sh invalid-key` — execution should stop at IF node (FALSE branch, no further nodes execute)
    8. Verify dedup: ACTIVATE the workflow (toggle Active), then send happy-path twice — second execution should stop at Remove Duplicates (0 items out)
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 steps pass. Describe which step failed if not.</resume-signal>
</task>

</tasks>

<verification>
After human checkpoint approval:

```bash
# Verify workflow JSON has all 5 ingestion nodes configured (not TODO stubs)
python3 -c "
import json
w = json.load(open('workflows/speed-to-lead-main.json'))
nodes = {n['name']: n for n in w['nodes']}
checks = {
  'webhook responseMode': nodes['Webhook: Google Ads Lead']['parameters']['responseMode'] == 'onReceived',
  'log has staticData': 'getWorkflowStaticData' in nodes['Code: Log Raw Payload']['parameters']['jsCode'],
  'IF reads body': '\$json.google_key' in json.dumps(nodes['IF: google_key valid?']['parameters']),
  'dedup cross-execution': 'removeItemsSeenInPreviousExecutions' in json.dumps(nodes['Remove Duplicates: lead_id']['parameters']),
  'extract parses array': 'user_column_data' in nodes['Code: Extract Lead Fields']['parameters']['jsCode'],
}
for check, result in checks.items():
    print(f'  {\"PASS\" if result else \"FAIL\"}: {check}')
assert all(checks.values()), 'Some checks failed'
print('All ingestion checks passed')
"
```
</verification>

<success_criteria>
- Webhook node has responseMode "onReceived" — returns 200 before processing
- Log node writes to staticData.lastLead AND console.log (both paths for INGEST-03 and NOTIF-04 prep)
- IF node reads google_key from $json (body), compares to $env.GOOGLE_KEY — not hardcoded
- Remove Duplicates uses removeItemsSeenInPreviousExecutions with lead_id key
- Extract code correctly parses user_column_data array into phone, email, name, request fields
- Human checkpoint: execution log shows correct field values for test lead
- Human checkpoint: invalid key stops execution at IF node
- Human checkpoint: duplicate lead_id stops execution at Remove Duplicates
</success_criteria>

<output>
After completion, create `.planning/phases/01-critical-path/01-03-SUMMARY.md`
</output>

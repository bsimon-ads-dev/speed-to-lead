---
phase: 03-whatsapp-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - config/dupont-plomberie.json
  - config/cabinet-martin.json
  - workflows/speed-to-lead-core.json
autonomous: true
requirements:
  - CHAN-01
must_haves:
  truths:
    - "Circuit breaker node is the first Code node in Core Workflow — runs before Log Raw Payload"
    - "More than 5 executions of same lead_id within 10 minutes sets circuit_breaker_tripped: true"
    - "When circuit_breaker_tripped is true, execution stops and a Twilio SMS alert fires to Baptiste"
    - "Both client configs declare whatsapp_enabled, whatsapp_template_sid, whatsapp_sender, owner_whatsapp_enabled, whatsapp_owner_template_sid fields"
    - "New env vars for WhatsApp (DUPONT_WHATSAPP_SENDER etc.) are documented in env_vars_required"
  artifacts:
    - path: "config/dupont-plomberie.json"
      provides: "WhatsApp config fields + env var registry"
      contains: "whatsapp_enabled"
    - path: "config/cabinet-martin.json"
      provides: "WhatsApp config fields + env var registry"
      contains: "whatsapp_enabled"
    - path: "workflows/speed-to-lead-core.json"
      provides: "Circuit breaker + halt branch wired before Log Raw Payload"
      contains: "circuit_breaker_tripped"
  key_links:
    - from: "Code: Circuit Breaker"
      to: "IF: Circuit Breaker Tripped?"
      via: "circuit_breaker_tripped boolean on json output"
    - from: "IF: Circuit Breaker Tripped? (true branch)"
      to: "HTTP Request: Twilio SMS (CB Alert)"
      via: "Twilio SMS to BAPTISTE_PHONE from client twilio credentials"
    - from: "When Executed by Another Workflow"
      to: "Code: Circuit Breaker"
      via: "first connection — replaces the direct link to Code: Log Raw Payload"
---

<objective>
Add WhatsApp config fields to both client configs and insert the circuit breaker as the first node in the Core Workflow.

Purpose: Establishes the config schema that Plan 02 reads from, and implements hardening SC-4 (halt and alert on more than 5 same lead_id in 10 min).

Output: Updated config JSONs with whatsapp_* fields and env var registry entries; updated Core Workflow JSON with circuit breaker Code node + halt IF node + alert SMS node wired before Log Raw Payload.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-whatsapp-hardening/03-RESEARCH.md

<interfaces>
Existing Core Workflow first connection (from workflows/speed-to-lead-core.json):
  "When Executed by Another Workflow" -> "Code: Log Raw Payload"

After this plan, the entry chain becomes:
  "When Executed by Another Workflow" -> "Code: Circuit Breaker"
    -> "IF: Circuit Breaker Tripped?"
        true branch  -> "HTTP Request: Twilio SMS (CB Alert)"  [execution ends here]
        false branch -> "Code: Log Raw Payload"  [existing flow unchanged from this point]

staticData pattern already in use (Code: Log Raw Payload):
  const staticData = $getWorkflowStaticData('global');
  staticData.lastLead = JSON.stringify(lead);
  Circuit breaker uses same API with key: staticData.circuitBreaker

Twilio HTTP Request node pattern (from node-twilio-prospect in core.json):
  method: POST
  url: =https://api.twilio.com/2010-04-01/Accounts/{{ $json.client_config.twilio_account_sid }}/Messages.json
  Authorization: =Basic {{ Buffer.from($json.client_config.twilio_account_sid + ':' + $json.client_config.twilio_auth_token).toString('base64') }}
  body keypairs: To, From, Body
  CB alert node reuses same pattern with $env.BAPTISTE_PHONE as To
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WhatsApp fields to client configs</name>
  <files>config/dupont-plomberie.json, config/cabinet-martin.json</files>

  <read_first>
    - config/dupont-plomberie.json
    - config/cabinet-martin.json
  </read_first>

  <action>
In config/dupont-plomberie.json, add the following five fields after "follow_up_enabled":

  "whatsapp_enabled": false,
  "owner_whatsapp_enabled": false,
  "whatsapp_sender": "",
  "whatsapp_template_sid": "",
  "whatsapp_owner_template_sid": ""

Values are false / empty string — WhatsApp is off by default until WABA onboarding completes per client. The actual E.164 number and ContentSids come from env vars assembled at runtime by the entry workflow (Plan 02 work).

In dupont-plomberie.json "env_vars_required", add these three entries:

  "DUPONT_WHATSAPP_SENDER": "E.164 phone number registered as WhatsApp sender in Twilio WABA — e.g. +33600000001 (NOT alphanumeric — alphanumeric IDs are SMS-only, rejected by WhatsApp)",
  "DUPONT_WHATSAPP_TEMPLATE_SID": "ContentSid from Twilio Content Template Builder after Meta approval — format HXabc... (prospect confirmation template speed_to_lead_confirm_fr)",
  "DUPONT_WHATSAPP_OWNER_TEMPLATE_SID": "ContentSid for owner notification WhatsApp template — format HXdef... (owner template speed_to_lead_owner_notif_fr)"

Apply identical changes to config/cabinet-martin.json with MARTIN_ prefix on env var keys:

  "MARTIN_WHATSAPP_SENDER": "E.164 phone number registered as WhatsApp sender in Twilio WABA — e.g. +33600000002 (NOT alphanumeric)",
  "MARTIN_WHATSAPP_TEMPLATE_SID": "ContentSid for prospect confirmation template — format HXabc...",
  "MARTIN_WHATSAPP_OWNER_TEMPLATE_SID": "ContentSid for owner notification template — format HXdef..."

Do NOT hardcode real ContentSids or real E.164 numbers in the config files — those are secrets stored in n8n env vars.
  </action>

  <acceptance_criteria>
    - config/dupont-plomberie.json contains all five keys: whatsapp_enabled, owner_whatsapp_enabled, whatsapp_sender, whatsapp_template_sid, whatsapp_owner_template_sid
    - config/cabinet-martin.json contains the same five keys
    - dupont-plomberie.json env_vars_required contains DUPONT_WHATSAPP_SENDER, DUPONT_WHATSAPP_TEMPLATE_SID, DUPONT_WHATSAPP_OWNER_TEMPLATE_SID
    - cabinet-martin.json env_vars_required contains MARTIN_WHATSAPP_SENDER, MARTIN_WHATSAPP_TEMPLATE_SID, MARTIN_WHATSAPP_OWNER_TEMPLATE_SID
    - Both files are valid JSON
  </acceptance_criteria>

  <verify>
    <automated>cd /Users/server/speed-to-lead && node -e "
const d = require('./config/dupont-plomberie.json');
const m = require('./config/cabinet-martin.json');
const waFields = ['whatsapp_enabled','owner_whatsapp_enabled','whatsapp_sender','whatsapp_template_sid','whatsapp_owner_template_sid'];
waFields.forEach(f => {
  if (!(f in d)) throw new Error('dupont missing: ' + f);
  if (!(f in m)) throw new Error('martin missing: ' + f);
});
['DUPONT_WHATSAPP_SENDER','DUPONT_WHATSAPP_TEMPLATE_SID','DUPONT_WHATSAPP_OWNER_TEMPLATE_SID'].forEach(k => {
  if (!(k in d.env_vars_required)) throw new Error('dupont env missing: ' + k);
});
['MARTIN_WHATSAPP_SENDER','MARTIN_WHATSAPP_TEMPLATE_SID','MARTIN_WHATSAPP_OWNER_TEMPLATE_SID'].forEach(k => {
  if (!(k in m.env_vars_required)) throw new Error('martin env missing: ' + k);
});
console.log('PASS');
"</automated>
  </verify>

  <done>Both config files contain whatsapp_* fields with false/empty defaults, and env_vars_required lists the three new WhatsApp env vars per client with descriptive comments about format requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Add circuit breaker node chain to Core Workflow</name>
  <files>workflows/speed-to-lead-core.json</files>

  <read_first>
    - workflows/speed-to-lead-core.json — existing nodes array and connections object
    - .planning/phases/03-whatsapp-hardening/03-RESEARCH.md — Pattern 3 (circuit breaker code)
  </read_first>

  <action>
Add three new nodes to the "nodes" array of workflows/speed-to-lead-core.json.

NODE 1 — Code: Circuit Breaker (id: "node-circuit-breaker")
Position: [240, 300] — same x as "When Executed by Another Workflow", shift that node left is NOT needed; instead position circuit breaker at [460, 300] to slot between trigger and log-raw, then shift node-log-raw to [680, 300] (it was at [460,300]).
Actually: keep existing x positions intact and insert circuit breaker at position [460, 300], moving Code: Log Raw Payload to [680, 300]. All downstream nodes shift by +220 in x.
Simpler approach: insert the new nodes but do NOT reposition existing nodes — n8n handles layout dynamically on import. Just set new node positions to something reasonable that does not overlap.

Use these positions:
  - "Code: Circuit Breaker": [240, 100]  (above the trigger, reads naturally as first step)
  - "IF: Circuit Breaker Tripped?": [460, 100]
  - "HTTP Request: Twilio SMS (CB Alert)": [680, 100]

NODE 1 jsCode (copy verbatim):
```
// Phase 3 SC-4: Circuit breaker — halt and alert on >5 same lead_id in 10 min
// Uses $getWorkflowStaticData('global') — same API as lastLead in Code: Log Raw Payload
const lead = $input.first().json;
const lead_id = lead.lead_id;

if (!lead_id) return $input.all(); // no lead_id — pass through unchanged

const staticData = $getWorkflowStaticData('global');
const WINDOW_MS = 10 * 60 * 1000; // 10 minutes
const THRESHOLD = 5;
const now = Date.now();

const tracker = staticData.circuitBreaker || {};

if (!tracker[lead_id]) {
  tracker[lead_id] = { count: 0, first_seen: now };
}

const entry = tracker[lead_id];

// Reset window if expired
if (now - entry.first_seen > WINDOW_MS) {
  entry.count = 0;
  entry.first_seen = now;
}

entry.count += 1;

// Prune entries older than 20 minutes to prevent unbounded staticData growth
for (const id of Object.keys(tracker)) {
  if (now - tracker[id].first_seen > 2 * WINDOW_MS) {
    delete tracker[id];
  }
}

staticData.circuitBreaker = tracker;

if (entry.count > THRESHOLD) {
  return [{ json: { ...lead, circuit_breaker_tripped: true, cb_count: entry.count } }];
}

return [{ json: { ...lead, circuit_breaker_tripped: false } }];
```

NODE 2 — IF: Circuit Breaker Tripped? (id: "node-cb-if")
Type: n8n-nodes-base.if, typeVersion: 2
Position: [460, 100]
Condition: leftValue="{{ $json.circuit_breaker_tripped }}", operator type:boolean operation:true
looseTypeValidation: false

NODE 3 — HTTP Request: Twilio SMS (CB Alert) (id: "node-cb-alert")
Type: n8n-nodes-base.httpRequest, typeVersion: 4
Position: [680, 100]
method: POST
url: =https://api.twilio.com/2010-04-01/Accounts/{{ $json.client_config.twilio_account_sid }}/Messages.json
Authorization header: =Basic {{ Buffer.from($json.client_config.twilio_account_sid + ':' + $json.client_config.twilio_auth_token).toString('base64') }}
bodyParameters keypairs:
  To: ={{ $env.BAPTISTE_PHONE }}
  From: ={{ $json.client_config.twilio_sender_id }}
  Body: =ALERTE circuit breaker: lead_id {{ $json.lead_id }} a declenche {{ $json.cb_count }} executions en 10 min. Execution stoppee. Client: {{ $json.client_config.client_slug }}

UPDATE the "connections" object:

1. Change "When Executed by Another Workflow" main[0] from pointing to "Code: Log Raw Payload" to pointing to "Code: Circuit Breaker"

2. Add new connection from "Code: Circuit Breaker":
   main[0] (output 0) -> "IF: Circuit Breaker Tripped?"

3. Add new connection from "IF: Circuit Breaker Tripped?":
   main[0] (true branch, output 0) -> "HTTP Request: Twilio SMS (CB Alert)"
   main[1] (false branch, output 1) -> "Code: Log Raw Payload"

4. "Code: Log Raw Payload" connection remains unchanged (still points to "Remove Duplicates: lead_id").

The node "HTTP Request: Twilio SMS (CB Alert)" has NO outgoing connection — execution terminates after the alert SMS.

Note on staticData and manual test executions: $getWorkflowStaticData('global') does NOT persist between manual test executions in the n8n editor. It only persists between production (webhook-triggered) executions. This is correct behavior — circuit breaker is a production defense, not a test constraint.
  </action>

  <acceptance_criteria>
    - workflows/speed-to-lead-core.json contains a node with id "node-circuit-breaker" and name "Code: Circuit Breaker"
    - The node's jsCode contains "circuit_breaker_tripped" and "circuitBreaker" and "THRESHOLD"
    - workflows/speed-to-lead-core.json contains a node with id "node-cb-if" and name "IF: Circuit Breaker Tripped?"
    - workflows/speed-to-lead-core.json contains a node with id "node-cb-alert"
    - The connections["When Executed by Another Workflow"] main[0][0].node equals "Code: Circuit Breaker"
    - The connections["IF: Circuit Breaker Tripped?"] main[1][0].node equals "Code: Log Raw Payload" (false branch goes to existing flow)
    - File is valid JSON
  </acceptance_criteria>

  <verify>
    <automated>cd /Users/server/speed-to-lead && node -e "
const wf = require('./workflows/speed-to-lead-core.json');
const nodes = wf.nodes;
const conns = wf.connections;

const cb = nodes.find(n => n.id === 'node-circuit-breaker');
if (!cb) throw new Error('missing node-circuit-breaker');
if (!cb.parameters.jsCode.includes('circuit_breaker_tripped')) throw new Error('circuit breaker code missing circuit_breaker_tripped');
if (!cb.parameters.jsCode.includes('THRESHOLD')) throw new Error('circuit breaker code missing THRESHOLD');

const cbIf = nodes.find(n => n.id === 'node-cb-if');
if (!cbIf) throw new Error('missing node-cb-if');

const cbAlert = nodes.find(n => n.id === 'node-cb-alert');
if (!cbAlert) throw new Error('missing node-cb-alert');

const triggerConn = conns['When Executed by Another Workflow'];
if (!triggerConn || triggerConn.main[0][0].node !== 'Code: Circuit Breaker') throw new Error('trigger not connected to circuit breaker');

const ifConn = conns['IF: Circuit Breaker Tripped?'];
if (!ifConn || ifConn.main[1][0].node !== 'Code: Log Raw Payload') throw new Error('false branch not connected to Log Raw Payload');

console.log('PASS — circuit breaker wired correctly');
"</automated>
  </verify>

  <done>Core Workflow JSON contains circuit breaker Code node, halt IF, and alert SMS node. Trigger points to circuit breaker as first node. False branch reconnects to existing Log Raw Payload flow. JSON is valid and importable into n8n.</done>
</task>

</tasks>

<verification>
Run both automated verify commands above. Both must print PASS.

Additionally grep for Body parameter on WhatsApp nodes as a pre-flight check before Plan 02:
  grep -n '"Body"' workflows/speed-to-lead-core.json
  Expected: finds Body params on existing SMS nodes (Prospect, Owner, Follow-up) only. Should return zero WhatsApp nodes at this stage.
</verification>

<success_criteria>
1. node -e "require('./config/dupont-plomberie.json')" and require('./config/cabinet-martin.json') both parse without error
2. Both configs contain the five whatsapp_* fields and three new env_vars_required entries per client
3. workflows/speed-to-lead-core.json is valid JSON with three new nodes (circuit-breaker, cb-if, cb-alert) and correct connections
4. The circuit breaker code uses $getWorkflowStaticData('global').circuitBreaker and threshold of 5 in 10-minute window
5. Alert SMS sends to $env.BAPTISTE_PHONE using the client's own Twilio credentials
</success_criteria>

<output>
After completion, create .planning/phases/03-whatsapp-hardening/03-01-SUMMARY.md
</output>

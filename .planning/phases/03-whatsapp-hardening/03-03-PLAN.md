---
phase: 03-whatsapp-hardening
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/TESTING.md
autonomous: false
requirements:
  - CHAN-01
must_haves:
  truths:
    - "Baptiste receives an alert email within 5 minutes if n8n stops responding"
    - "TESTING.md documents the Phase 3 test procedures: circuit breaker test, WhatsApp delivery verification, UptimeRobot setup verification"
    - "UptimeRobot monitors the production webhook URL (not webhook-test)"
  artifacts:
    - path: "tests/TESTING.md"
      provides: "Phase 3 test procedures"
      contains: "Phase 3"
  key_links:
    - from: "UptimeRobot HTTP monitor"
      to: "https://[n8n-host]/webhook/dupont-plomberie"
      via: "5-minute check interval, email alert on non-2xx or timeout"
---

<objective>
Configure UptimeRobot monitoring for n8n uptime and add Phase 3 test procedures to TESTING.md.

Purpose: Implements SC-3 (5-min uptime alert) and documents how to verify all Phase 3 success criteria. UptimeRobot requires a human checkpoint — no CLI access.

Output: UptimeRobot monitor live and alerting Baptiste's email; TESTING.md updated with Phase 3 section covering circuit breaker test, WhatsApp delivery test, and UptimeRobot verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-whatsapp-hardening/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 3 test procedures to TESTING.md</name>
  <files>tests/TESTING.md</files>

  <read_first>
    - tests/TESTING.md — existing content and structure to preserve and extend
    - .planning/phases/03-whatsapp-hardening/03-RESEARCH.md — Pattern 5 (UptimeRobot), circuit breaker staticData behavior, Pitfall 5 (webhook-test vs webhook URL)
  </read_first>

  <action>
Append a new "## Phase 3: WhatsApp + Hardening" section to tests/TESTING.md. Do not modify existing Phase 1 or Phase 2 sections.

The new section must cover these four test scenarios:

---
## Phase 3: WhatsApp + Hardening

### Test 3-A: Circuit Breaker (SC-4)

**What it tests:** More than 5 executions of the same lead_id within 10 minutes halts execution and sends alert SMS to Baptiste.

**Prerequisite:** n8n running. Core Workflow activated. BAPTISTE_PHONE env var set. Circuit breaker nodes present in Core Workflow (Plan 03-01).

**Note on staticData:** $getWorkflowStaticData('global') does NOT persist between manual (editor) test executions. The circuit breaker only fires across production webhook-triggered executions. Use the webhook URL (not webhook-test).

**Steps:**
1. Activate the Core Workflow and dupont-plomberie entry workflow in n8n (not test mode — production activation)
2. Send 7 identical webhook calls using the same lead_id:
   for i in $(seq 1 7); do GOOGLE_KEY=your_key ./tests/test-webhook.sh happy dupont-plomberie; sleep 2; done
   (Replace your_key with DUPONT_GOOGLE_KEY value)
3. Check n8n Executions view — executions 6 and 7 should show "circuit_breaker_tripped: true" in the circuit breaker Code node output
4. Check Baptiste's phone — should receive alert SMS: "ALERTE circuit breaker: lead_id ... a declenche X executions en 10 min..."

**Expected result:** Executions 1-5 proceed normally (prospect SMS sent). Execution 6+ is halted at circuit breaker with alert SMS to Baptiste.

**RESTORE:** After test, wait for the 10-minute window to expire before running production tests, or manually clear staticData.circuitBreaker in n8n Settings > Workflow static data.

---

### Test 3-B: WhatsApp Prospect Message (SC-1)

**What it tests:** Prospect receives WhatsApp message via approved template when whatsapp_enabled is true.

**Prerequisite:** WABA onboarding complete for the test client (DUPONT_WHATSAPP_SENDER env var set, DUPONT_WHATSAPP_TEMPLATE_SID set to approved ContentSid). Entry workflow has whatsapp_enabled: true set in Code: Assemble Client Config.

**Steps:**
1. Temporarily set whatsapp_enabled: true in entry workflow jsCode for dupont-plomberie
2. Send a test lead: GOOGLE_KEY=your_key ./tests/test-webhook.sh happy dupont-plomberie
3. Check Twilio Console > Monitor > Messaging Logs — the outbound WhatsApp message should show status "delivered" with a message SID starting with MM
4. Check the prospect's WhatsApp — should receive the template message: "Bonjour [name], votre demande a bien ete recue. Dupont Plomberie vous rappelle sous 30 minutes."

**IMPORTANT:** Never add a Body parameter to the WhatsApp HTTP Request node. If the message arrives as free-form text (not a template), the node is misconfigured. Check Twilio Console for delivery receipts — absence of delivery receipt with an MM SID means silent delivery failure (likely Body used instead of ContentSid).

**Restore:** Set whatsapp_enabled back to false after testing unless activating for production.

---

### Test 3-C: WhatsApp Owner Notification (SC-2)

**What it tests:** Owner receives WhatsApp notification when owner_whatsapp_enabled is true.

**Prerequisite:** WABA onboarding complete. DUPONT_WHATSAPP_OWNER_TEMPLATE_SID env var set to approved ContentSid. Entry workflow has owner_whatsapp_enabled: true.

**Steps:**
1. Temporarily set owner_whatsapp_enabled: true in entry workflow jsCode
2. Send a test lead: GOOGLE_KEY=your_key ./tests/test-webhook.sh happy dupont-plomberie
3. Check Twilio Console — outbound WhatsApp to owner_phone should appear with "delivered" status
4. Check owner's WhatsApp — should receive: "Nouveau lead: [name] — Demande: [request] — Tel: [phone]"

**Restore:** Set owner_whatsapp_enabled back to false after testing.

---

### Test 3-D: UptimeRobot Alert (SC-3)

**What it tests:** Baptiste receives alert email within 5 minutes of n8n stopping.

**Prerequisite:** UptimeRobot monitor configured (see checkpoint task in Plan 03-03). n8n running in production.

**Steps:**
1. Stop n8n (Railway: pause deployment or disable the service temporarily)
2. Wait up to 5 minutes (free tier checks every 5 minutes)
3. Check Baptiste's email for UptimeRobot alert: "Your monitor [n8n dupont-plomberie webhook] is DOWN"
4. Restart n8n
5. Wait for UptimeRobot recovery email

**Manual only** — cannot be automated (requires stopping the production service).

**Common mistake:** If the monitor URL uses /webhook-test/ instead of /webhook/, UptimeRobot will fire false-positive alerts constantly (test URL only responds when workflow is open in editor). Verify the monitor URL uses /webhook/ (production path).

---
  </action>

  <acceptance_criteria>
    - tests/TESTING.md contains the text "Phase 3" and "Circuit Breaker"
    - tests/TESTING.md contains the text "whatsapp_enabled"
    - tests/TESTING.md contains the text "UptimeRobot"
    - tests/TESTING.md contains the text "RESTORE" warning in the circuit breaker section
    - Existing Phase 1 and Phase 2 sections are intact
  </acceptance_criteria>

  <verify>
    <automated>cd /Users/server/speed-to-lead && grep -c "Phase 3" tests/TESTING.md && grep -c "Circuit Breaker" tests/TESTING.md && grep -c "whatsapp_enabled" tests/TESTING.md && grep -c "UptimeRobot" tests/TESTING.md && echo "PASS — Phase 3 section present"</automated>
  </verify>

  <done>TESTING.md contains Phase 3 section with four test scenarios covering circuit breaker (with RESTORE warning), WhatsApp prospect, WhatsApp owner, and UptimeRobot alert. Existing content is unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UptimeRobot monitor setup</name>
  <files>N/A — external service configuration</files>

  <action>Human sets up UptimeRobot HTTP(s) monitor for n8n webhook URL and sets Anthropic spend cap. No automated steps — UptimeRobot has no CLI.</action>

  <what-built>
Plan 03-01 and 03-02 added circuit breaker and WhatsApp branches to the Core Workflow. This checkpoint configures external uptime monitoring — the only step in Phase 3 that requires human interaction in a web UI (UptimeRobot has no CLI and no API key is set up).
  </what-built>

  <how-to-verify>
Complete these steps in order:

**1. Create UptimeRobot account (if not already)**
   Go to https://uptimerobot.com — sign up free. No credit card required.

**2. Create the HTTP(s) monitor**
   Dashboard > Add New Monitor
   - Monitor Type: HTTP(s)
   - Friendly Name: n8n dupont-plomberie webhook
   - URL: https://[YOUR-N8N-HOST]/webhook/dupont-plomberie
     (Replace [YOUR-N8N-HOST] with your Railway n8n hostname, e.g. speed-to-lead.up.railway.app)
     IMPORTANT: Use /webhook/ NOT /webhook-test/ — test URL returns 404 in production
   - Monitoring Interval: 5 minutes (free tier maximum)
   - Alert Contacts: Add your email address

**3. Verify monitor is running**
   - Status should show "UP" with a green indicator within 2 minutes of creation
   - If status shows "DOWN" immediately, the n8n workflow is not activated — activate the dupont-plomberie entry workflow in n8n first

**4. Verify alert configuration**
   - Click the monitor > Edit > Alert Contacts — confirm Baptiste's email is listed
   - Alert delay: 0 (alert immediately on first failure, not after 2-3 failures)

**5. Test alert delivery (optional but recommended)**
   - Temporarily pause the monitor, then un-pause and verify email arrives within 5 minutes
   - OR: pause the Railway n8n service for 6 minutes, check email for DOWN alert, then restart

**Anthropic spend cap (30 seconds — console only)**
   While in this session, also set the Anthropic spend cap:
   - Go to https://console.anthropic.com > Settings > Limits
   - Set customer-configured spend limit to $20/month
   - This is a safety net against runaway bugs — current spend is ~$0.30/month, $20 is 65x headroom

**Confirm both are done:**
  - UptimeRobot: monitor shows UP, email alert contact configured
  - Anthropic: $20/month spend limit set
  </how-to-verify>

  <resume-signal>Type "done" once UptimeRobot monitor is UP and Anthropic spend cap is set. Or describe any issue encountered.</resume-signal>

  <verify>
    <automated>MISSING — manual verification only. UptimeRobot has no CLI. Confirm visually: monitor shows UP status in UptimeRobot dashboard.</automated>
  </verify>

  <done>UptimeRobot monitor status is UP targeting /webhook/ URL with 5-min interval and email alert. Anthropic spend limit is $20/month.</done>
</task>

</tasks>

<verification>
After both tasks:
1. grep -c "Phase 3" tests/TESTING.md -- returns >= 1
2. UptimeRobot dashboard shows monitor status "UP" for n8n dupont-plomberie webhook
3. Anthropic Console shows $20/month spend limit
</verification>

<success_criteria>
1. tests/TESTING.md contains Phase 3 section with all four test scenarios (circuit breaker, WhatsApp prospect, WhatsApp owner, UptimeRobot)
2. RESTORE warning present in circuit breaker test to prevent accidental production interference
3. UptimeRobot HTTP(s) monitor targeting /webhook/ (not /webhook-test/) URL, 5-min interval, email alert configured
4. Anthropic spend cap set to $20/month in console
</success_criteria>

<output>
After completion, create .planning/phases/03-whatsapp-hardening/03-03-SUMMARY.md
</output>
